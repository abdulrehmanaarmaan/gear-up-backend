model Category {
    id          String      @id @default(cuid())
    name        String      @unique
    slug        String      @unique
    description String?
    image       String?
    gearItems   GearItem[]
    createdAt   DateTime    @default(now())
    updatedAt   DateTime    @updatedAt
    gearItemsId String

    @@map("categories")
}
