enum UserRole {
    CUSTOMER
    PROVIDER
    ADMIN
}

enum UserStatus {
    ACTIVE
    SUSPENDED
}

enum GearCondition {
    NEW
    EXCELLENT
    GOOD
    FAIR
}

enum GearStatus {
    PENDING
    APPROVED
    REJECTED
    HIDDEN
}

enum RentalStatus {
    PLACED
    CONFIRMED
    PAID
    PICKED_UP
    RETURNED
    CANCELLED
}

enum PaymentMethod {
    CARD
    BANK
    MOBILE_BANKING
}

enum PaymentProvider {
    STRIPE
    SSLCOMMERZ
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
    REFUNDED
}
